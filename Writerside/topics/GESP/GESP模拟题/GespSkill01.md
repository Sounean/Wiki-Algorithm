# Gesp三级大题技巧总结

## 1.大题第一大题

他考纲就限定死了大题考一维数组！所以肯定题目是在一维数组里面进行找特殊的值！全部题型，最终只会分成以下两种:

### 1.1。找一个特殊值的题或者边遍历边处理数组的题：

在数组中找到某个特殊要求的值，然后对数组进行处理
```C++
对数组a[num]:
for(int i=0;i<num;i++){
    //  去寻找特殊要求的值
}
// 对数组进行处理
```
B3661:思路：就是遍历，然后找到``数组中奇数和偶数``,对应进行处理输出即可<br/>
B3842:思路：这题难点是创建数组的角度！其实也就是遍历来``寻找为0的元素``<br/>
B2095:思路：就是遍历，然后找到``最大/最小值``，然后再进行处理加工数据。<br/>
B3867:思路：单纯一个模拟算法，模拟现实生活的场景，拿纸笔，先用数学方式对着输入输出样例能写出来即可<br/>
如副本2412的大题1：思路：遍历去求``最大值、最小值``(或者用sort去耍赖求最值)，然后对数组进行处理，最后输出。<br/>
如副本2409的大题1：思路：题中要满足存在序列t，以t为分界线的所有左边的元素(包括t)，的和要等于t右边的和，则输出yes。那么就是遍历这个数组，
然后在每次循环中去计算从开头一直到t的和，来看是否是数组总和的一半，是一半，则是符合的<br/>
如副本2406的大题2：思路：求数组是否存在一个数是所有元素的倍数。那么就直接sort去求最大值先。然后遍历数组，去每个元素比过去，看下是否余数为0即可<br/>
如B3868:思路：是属于边遍历边处理的问题，直接for循环遍历拿到的数组，然后根据条件处理即可。<br/>


### 1.2.找多个特殊值的题：

对a数组，自己数组内两两元素全部比较过去，求最值或者符合条件的值。
这种情况下，怎么有序且不重复的把所有该数组中任意两个元素依次可以遍历对比过去呢？用以下公式:
```C++
有数组a[num]：
for (int i = 0; i < num; ++i) {
        for (int k=i+1;k<num;k++) {
            // a[i]和a[k]的处理
            }
        }
    }
```

如题P5728 (别管什么用两个数组什么数组的，能有最狡猾的就用最狡猾的 ٩(ˊᗜˋ*)و)
如副本2403的大题2：思路：因为他是需要对a数组找出任意两个元素的，就用这个公式，在处理中，去判断是否有满足条件的。

# 大题第二大题
字符串相关
### 2.1.检测是否合规
（检查字符串中的字符是否符合某种范围(比如大小写、是否是'0'~'9'')，根据ASC2码值范围去搞）:
首先要做的肯定是遍历，公式如下(几乎所有！是所有！都要用到)：
```C++
string str = "";
cin >> str;
for(int i=0;i<str.length();i++){
    // str[i]即字符串上的每一个字符了。
}
```
B356:单纯就是检查是否合规的一种变体，检查``是否是大小写``<br/>
B3868:就是遍历获取的字符串，然后判断``属于哪个ASC2码范围``<br/>
B2120:就是遍历字符串，然后碰到``空格``该怎么处理<br/>
B3868:就是遍历获取的字符串，然后判断``属于哪个ASC2码范围``<br/>

### 2.2.寻找字符串中的特殊子串：
(要找字符串中的特殊字符的话，直接for遍历就好了，或者find函数，这里不再特别赘述)<br/>
这个时候就要用上字符串函数了：
```C++
#include "string"

// 要写上find、substr、erase
```


B3843:很值得自己从头到尾做一次，这个可以踩很多坑，训练思维耐久度。最难的不过这题了。就是先找特殊子串，然后再检查是否合规。<br/>
B3769:就差直接写出来了，让你用```substr函数```;
B4039:就是遍历```所有子串可能出现的长度```然后去看是否是回文串
