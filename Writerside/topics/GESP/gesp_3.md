# GESP 3级

## 1.数据编码 （原码、反码、补码）
考点解读:
理解计算机中数值的二进制表示方式，包括原码（符号位+绝对值）、反码（符号位不变，其余位取反）、补码（反码+1，用于简化加减法运算）。

需掌握三者间的转换规则及其应用场景（如补码用于表示负数）。

0.先将数值转换成2进制的表示形式，原反补码均是对于2进制的表现形式来说的。<br/>
1.原码反码补码三种表示方式均由符号位和数值位两部分组成。符号位为第1位(0为正号，1为负号)，后面的均为数值位。<br/>
[+1] = [0000 0001]原;[-1] = [1000 0001]原; 以十进制值转换成转换成8位二进制为例子。<br/>
2.正数的原反补码均一样。只有负数，他的原反补需要进行推导来求出。<br/>
3.负数的反码为原码的符号位不变，其余各位按位取反。补码为反码+1。<br/>
[+1] = [00000001]原 = [00000001]反; [-1] = [10000001]原 = [11111110]反;<br/>
[+1] = [00000001]原 = [00000001]反 = [00000001]补; [-1] = [10000001]原 = [11111110]反 = [11111111]补<br/>
4.8位二进制数能表示的十进制数的范围为-128~127;
5.特殊情况记住:以8位2进制为例:原码0可以是0000 0000或1000 0000；反码0可以是0000 0000或1111 1111；只有补码的0的表示才是唯一的:0000 0000<br/>
6.为啥补码的0的表示方式就一个？因为1000 0000是补码中特殊的编码，当作-128了。（补码统一了加减法运算，避免 -0 的二义性，并扩展负数范围（8 位补码范围：-128 ~ 127）(可不记住原理，只记住最终结论)

一些题目中会出现的专业术语:
1.“真值”：“补码的真值”指的是补码所表示的实际数值（即人类直观理解的十进制数）；机器数对应的实际数值（如 10000011 的真值是 -3，而非形式值131）；<br/>
2.“机器数”:指数值在计算机内部的二进制表示形式。<br/>
1和2的关键区别:机器数是“表象”（二进制编码），真值是“本质”（人类理解的数值）。例如：

机器数 00000101 → 真值 +5<br/>
机器数 11111011 → 真值 -5（补码表示）

<warning>锐评：近几年考频突增!!!!本质上不难，第一次学要花时间久，后面复习都是10min内就可以过一轮的知识点</warning>

## 2.进制转换  (二、八、十、十六进制)
考点解读：
熟练掌握不同进制间的转换方法，

如十进制转二进制（除2取余）、二进制转十六进制（4位分组）等，需熟悉各进制的表示规则（如十六进制用A-F表示10-15）。



## 3.位运算 （与、或、非、异或、左移、右移）
考点解读:
掌握位运算符（&、|、~、^、<<、>>）的功能及应用，如掩码操作、快速乘除2、异或找不同等。

<note>这个是很简单的，后面复习花的时间非常短。可以说是3级考点里最简单的。</note>

## 4.算法与描述（枚举法、模拟法；自然语言/流程图/伪代码）
考点解读：
能用枚举法穷举所有可能解（如排列组合），模拟法按步骤实现逻辑。需掌握流程图和伪代码的规范描述。
练习题单：
[P1036 [NOIP2002 普及组] 选数]（枚举+素数判断）
[P1563 [NOIP2016 提高组] 玩具谜题]（模拟环形队列）

## 5.数据结构（C++一维数组）
python中用的列表，列表里面元素变量类型可以不同，但是在C++中需要变量类型一致。
```C++
写法标准:
1. 定义时：
注：数组长度只能是大于0的整数，且不能是变量和变量表达式
int a[5];   // 可以
int a['0']; // 也可以！ 这里面他本质上是将'0'隐式转换成了对应的ASC2编码值48了，该式等价于int a[48]!
int a['A']; // 也可以！ 这里等价于int a[65];
int n = 5;
int a[n];   // 不可以，因为n是变量了
int a[n+2]; // 步可以，因为n是变量表达式了

2.初始化：
int a[5] = {98,88,85,78,92};    // 可以
int a[5] = {98,88,85,78,92,11}; // 不可以，最后一个元素11写在a数组的控制范围之外了。

3.获取数组中某个元素时:
int a[5] = {98,88,85,78,92}; 
cout << a[0];   // 返回98
cout << a[-1];  // python会返回序列最后一个元素，但C++不会！，编译不会出错，但运行时会给你一个意想不到的值
cout << a[99];  // 下标为负数和超过数组长度统称下标越界。编译不会出错但运行时会给你一个意想不到的值

4.赋值时:
int a[5] = {};
a = {1,2,3,4,5};    // 错误！数组名不是变量，不能通过数组名去直接给数组赋值
a[0] = 1;
a[4] = 9;   // 这两行可以

5.小tips:
数组的值会随着定义的位子不同而不同。
如果是:
int a[5];
int main(){...} // 此时a里面有5个元素，值均是0

int main(){int a[5];...}    // 此时a数组里面是该片内存遗留的垃圾数据（不推荐这么些！）
```
其他创建方式
![具体效果结果](gesp_3_pic01.png "具体效果结果")

考试的时候，一般都是这么处理：
如果这个题目告诉你，需要承接一个数组，数组内元素不大于100，那么可以写int a[110];稍微比所需要的元素数量再大
一些，方便后面不时之需。

此处要明白,自己要能解释以下3个疑惑：
```C++
1.c++中，长度为n的数组，合理的下标范围为什么是从0~n-1?

2.为什么c++中可以用'a'字符来作为下标？他等价于取第几个元素？

3.C++中数组的下标从多少开始计数?

4.c++中，访问下标为n的元素会不会引起编译错误?

5.字符数组的最后一个字符本身是没有限制的，但如果是用于保存字符串时，则必须用字符'\0'结尾。
```

还有一个考点是数组所占内存大小计算.

考点解读：
熟练使用一维数组进行存储、遍历、排序和查找操作，理解数组下标从0开始的特点。
练习题单：
[P1046 [NOIP2005 普及组] 陶陶摘苹果]
[P1427 小鱼的数字游戏] (数组逆序输出)

<note>不会直接考性质！，会给你代码，让你去回答执行结果是如何，占分非常大。(可偷分)</note>

## 6.字符串及其函数（大小写转换、搜索、分割、替换）
考点解读：
掌握C++字符串函数（如tolower()、find()、substr()、replace()）的使用，能处理字符串的常见操作。
练习题单：
[P1308 [NOIP2011 普及组] (统计单词数]字符串搜索与转换）
[P1598 垂直柱状图]（字符统计与格式化输出）

字符串初始化

遍历字符串

字符串输入


## 7.ASC2编码
直接看字符那一章节也行

<note>那是相当的简单，只要记住'A','a'和'0'对应的ASC2编码值是多少就好了。</note>

## 综合练习建议
优先级：先掌握进制转换和位运算（高频考点），再强化枚举与模拟法（解决实际问题）。
难度递进：从基础题（如P1046）过渡到中等题（如P1036），最后挑战复杂逻辑题（如P5657）。
代码规范：注意伪代码描述的清晰性，养成写注释的习惯。