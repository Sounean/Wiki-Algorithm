# P2142_高精减

有了高精度加做铺垫后，在高精度加的思想基础上去完成减，如果发现做不来的可以回退到高进度加，在草稿纸上去理下高精度加写题的思路和为什么这么去做。

高精度减容易产生WA(Wrong Answer:答案错误)和RE(Runtime Error:运行错误，比如数组越界、非void函数没返回值那些)，注意必须要把下面测试集全过了
才算AC。

## 1.题目描述
高精度减法。

## 2.输入输出
### 2.1.输入格式
两个整数 a,b（第二个可能比第一个大）。

### 2.2.输出格式
结果（是负数要输出负号）。

### 2.3.输入输出样例

#### 2.3.1测试集1
#### 输入
```C++
2
1
```

### 输出
```C++
1
```

#### 2.3.2测试集2
#### 输入
```C++
100
1
```

### 输出
```C++
99
```

#### 2.3.3更多测试集
```C++
测试用例1 - 相等的数：
输入: 123 123
期望输出: 0

测试用例2 - 第二个数更大：
输入: 123 456
期望输出: -333

测试用例3 - 需要借位：
输入: 1000 1
期望输出: 999

测试用例4 - 前导零：
输入: 1000 999
期望输出: 1

测试用例5 - 大数测试：
输入: 999999999999999999999999999999 1
期望输出: 999999999999999999999999999998

测试用例6 - 位数差异很大：
输入: 1000000000000000000000000000000 999999999999999999999999999999
期望输出: 1

测试用例7 - 全是9的借位：
输入: 10000 9999
期望输出: 1 
```

## 3.C++代码实现
依赖高精度加的方式去做，去给中间每位加的逻辑换成每位减的；然后注意一些边际条件即可(1.后一个数比前一个大，需要反过来减，然后加上负号;2.如果算出来
之后，有些0需要省略)
```C++
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

// 比较两个正整数字符串的大小
// 返回: 1表示a>b, -1表示a<b, 0表示a==b
int compare(const string& a, const string& b) {
    if (a.length() > b.length()) return 1;
    if (a.length() < b.length()) return -1;
    return a.compare(b);
}

// 高精度减法，假设a >= b >= 0
string subtract(const string& a, const string& b) {
    vector<int> num1, num2, result;

    // 将字符串转换为数字数组（逆序存储）
    for (int i = a.length() - 1; i >= 0; i--) {
        num1.push_back(a[i] - '0');
    }
    for (int i = b.length() - 1; i >= 0; i--) {
        num2.push_back(b[i] - '0');
    }

    // 补齐较短的数组
    while (num2.size() < num1.size()) {
        num2.push_back(0);
    }

    // 执行减法
    int borrow = 0;
    for (int i = 0; i < num1.size(); i++) {
        int diff = num1[i] - num2[i] - borrow;
        if (diff < 0) {
            diff += 10;
            borrow = 1;
        } else {
            borrow = 0;
        }
        result.push_back(diff);
    }

    // 去除前导零
    while (result.size() > 1 && result.back() == 0) {
        result.pop_back();
    }

    // 转换回字符串
    string ans = "";
    for (int i = result.size() - 1; i >= 0; i--) {
        ans += (char)(result[i] + '0');
    }

    return ans;
}

int main() {
    string a, b;
    cin >> a >> b;

    int cmp = compare(a, b);

    if (cmp == 0) {
        cout << "0" << endl;
    } else if (cmp > 0) {
        cout << subtract(a, b) << endl;
    } else {
        cout << "-" << subtract(b, a) << endl;
    }

    return 0;
}
```
{collapsible="true" collapsed-title="具体实现"}

拔高：
```C++
1.使用 vector 动态数组: 避免固定大小限制
2.简化比较逻辑: 使用更清晰的字符串比较
3.正确的前导零处理: 确保结果不包含前导零
4.清晰的借位逻辑: 使用标准的减法借位算法
```

```C++
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

// 比较两个正整数字符串的大小
// 返回: 1表示a>b, -1表示a<b, 0表示a==b
int compare(const string& a, const string& b) {
    if (a.length() > b.length()) return 1;
    if (a.length() < b.length()) return -1;
    return a.compare(b);    // 巧妙的使用了字符串他本身大小的比较来省去每位比较的逻辑
}

// 利用vector多次省掉赘述步骤
// 高精度减法，假设a >= b >= 0
string subtract(const string& a, const string& b) {
    vector<int> num1, num2, result;
    
    // 将字符串转换为数字数组（逆序存储）
    for (int i = a.length() - 1; i >= 0; i--) {
        num1.push_back(a[i] - '0');
    }
    for (int i = b.length() - 1; i >= 0; i--) {
        num2.push_back(b[i] - '0');
    }
    
    // 补齐较短的数组
    while (num2.size() < num1.size()) {
        num2.push_back(0);
    }
    
    // 执行减法
    int borrow = 0;
    for (int i = 0; i < num1.size(); i++) {
        int diff = num1[i] - num2[i] - borrow;
        if (diff < 0) {
            diff += 10;
            borrow = 1;
        } else {
            borrow = 0;
        }
        result.push_back(diff);
    }
    
    // 去除前导零
    while (result.size() > 1 && result.back() == 0) {
        result.pop_back();
    }
    
    // 转换回字符串
    string ans = "";
    for (int i = result.size() - 1; i >= 0; i--) {
        ans += (char)(result[i] + '0');
    }
    
    return ans;
}

int main() {
    string a, b;
    cin >> a >> b;
    
    int cmp = compare(a, b);
    
    if (cmp == 0) {
        cout << "0" << endl;
    } else if (cmp > 0) {
        cout << subtract(a, b) << endl;
    } else {
        cout << "-" << subtract(b, a) << endl;
    }
    
    return 0;
}
```
{collapsible="true" collapsed-title="具体实现"}